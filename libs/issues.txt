
 KNOWN ISSUES

 - bzip2 compiles as a static library only
 - imlib2 compiles as a static library only
 - sdl_mixer uses smpeg for mp3s, which is very very slow
 - mikmod attempts to create music.raw if /dev/dsp is not available ?? (in pygame anyway)
